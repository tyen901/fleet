{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Fleet UI (dx serve)",
      "type": "node",
      "request": "launch",
      "runtimeExecutable": "dx",
      "args": ["serve"],
      "cwd": "${workspaceFolder}/apps/fleet",
      "console": "integratedTerminal"
    }
  ]
}
