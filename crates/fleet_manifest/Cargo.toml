[package]
name = "fleet_manifest"
version = "0.1.0"
edition = "2021"

[features]
default = ["swifty"]
swifty = ["dep:fleet_types"]

[dependencies]
thiserror = "1"
fleet_types = { path = "../fleet_types", optional = true }
relative-path = { workspace = true }
