[package]
name = "fleet-ui"
version = "0.1.0"
edition = "2021"

[dependencies]
# New backend you showed (underscore name)
fleet_app = { path = "../fleet_app" }
coordinator = { path = "../coordinator" }

eframe = "0.33.3"
egui = "0.33.3"
rfd = "0.15"

tokio = { version = "1.43", features = ["rt-multi-thread", "macros", "sync"] }

