[package]
name = "fleet_index"
version = "0.1.0"
edition = "2021"

[dependencies]
blake3 = "1"
rusqlite = { version = "0.30", features = ["bundled"] }
thiserror = "1"

[dev-dependencies]
tempfile = "3"

