[package]
name = "fleet_index"
version = "0.1.0"
edition = "2021"

[dependencies]
blake3 = "1"
rusqlite = { version = "0.30", features = ["bundled"] }
thiserror = "1"
fleet_fs = { path = "../fleet_fs" }

[dev-dependencies]
tempfile = "3"
