[package]
name = "sync_plan"
version = "0.1.0"
edition = "2021"

[dependencies]
manifest_types = { path = "../manifest_types" }
relative-path = "1.9"
serde = { version = "1.0", features = ["derive"] }
